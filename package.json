{
  "name": "todo-app",
  "version": "0.0.1",
  "description": "My digital academy to do app",
  "main": "index.html",
  "scripts": {
    "version": "git add package.json",
    "postversion": "git push && git push --tags",
    "start": "python -m SimpleHTTPServer",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "js-cookie": "^2.2.0"
  }
}
